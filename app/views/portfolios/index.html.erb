<div class="album text-muted">
  <div class="container">
    <div class="row">

      <!-- <p><%= link_to "Add", new_portfolio_path if logged_in?(:site_admin) %></p> -->

      <%= render partial: 'portfolio_item', collection: @portfolio_items %>
    </div>
  </div>
</div>